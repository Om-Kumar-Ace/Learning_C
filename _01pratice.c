#include<stdio.h>

int main()
{
    int length,breadth ;
    printf("The length of rectangle is  \n");
    scanf("%d", &length);

    printf("The breadth of rectangle is %d \n");
    scanf("%d", &breadth);

    printf("Area of rectangle is %d \n", length*breadth);

    return 0;
}